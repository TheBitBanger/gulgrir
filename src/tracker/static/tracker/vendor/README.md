# Vendor Assets

This directory stores third-party browser assets synced from npm packages.

- `htmx.min.js` from `htmx.org`
- `flatpickr.min.js` and `flatpickr.min.css` from `flatpickr`

To refresh vendor assets after dependency updates, run:

`npm run sync:vendor`
